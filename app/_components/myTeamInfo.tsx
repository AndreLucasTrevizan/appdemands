'use client';

import { Card, CardBody, CardHeader, Divider } from "@heroui/react";
import { useState } from "react";

export default function MyTeamInfo() {
  const [] = useState();

  return (
    <Card>
      <CardHeader>
        <div>
          <h2>Equipe X</h2>
          <small>Equipe de Serviço</small>
        </div>
      </CardHeader>
      <Divider />
      <CardBody>
        
      </CardBody>
    </Card>
  );
}
